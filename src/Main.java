public class Main {
    
    public void main() {
        User user1 = new User(null);
        user1.chooseName();
        
    }
    /*
     * Item{
     * unock(Key test)
     * if id==tes.id{
     *  unlock --> true
     * }else{
     * no can do  -- false
     * }
     * 
     * 
     * action?  --> unlock chest
     * currentItem= chest
     * what key? -->2
     * usedKey= items.get(??)
     * success= currentItem.unlock(usedKey)
     * currentItem.setStatus(success)
     * }
     */

}

